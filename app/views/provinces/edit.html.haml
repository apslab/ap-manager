- content_for(:title) do
  %h1= t('scaffold.actions.edit', :item => Province.model_name.human).titlecase

= render 'form'

= link_to t('scaffold.actions.show').titlecase, @province
\|
= link_to t('scaffold.actions.back').titlecase, provinces_path

