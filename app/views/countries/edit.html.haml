- content_for(:title) do
  %h1= t('scaffold.actions.edit', :item => Country.model_name.human).titlecase

= render 'form'

= link_to t('scaffold.actions.show').titlecase, @country
\|
= link_to t('scaffold.actions.back').titlecase, countries_path

