<%= form_for @phone_type do |f| %>
	<% if @phone_type.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@phone_type.errors.count, "error") %> prohibido guardar Tipo de Teléfono:</h2>
			<ul>
			<% @phone_type.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
			</ul>
		</div>
	<% end %>
	<p>
		<%= f.label :name %><br />
		<%= f.text_field :name %>
	</p>
	<p>
		<%= f.label :code %><br />
		<%= f.text_field :code %>
	</p>
	<p>
		<%= f.label :default %><br />
		<%= f.check_box :default %>
	</p>
	<p><%= f.submit %></p>
<% end %>
